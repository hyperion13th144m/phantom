{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Patent Document Schema",
            "type": "shell",
            "command": "${workspaceFolder}/schema/build-all.sh"
        }
    ]
}