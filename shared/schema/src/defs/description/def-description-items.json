{
    "$comment": "tags under description that directly contain paragraph-blocks",
    "type": "object",
    "properties": {
        "tag": {
            "type": "string",
            "enum": [
                "technical-field",
                "background-art",
                "description-of-drawings",
                "best-mode",
                "mode-for-invention",
                "industrial-applicability",
                "reference-signs-list",
                "reference-to-deposited-biological-material",
                "sequence-list-text"
            ]
        },
        "jp-tag": {
            "type": "string"
        },
        "indent-level": {
            "type": "string"
        },
        "blocks": {
            "type": "array",
            "items": {
                "$comment": "blocks contains 'header' element. currently ignored, due to lack of no data using heading",
                "$ref": "#/$defs/paragraph"
            }
        }
    },
    "required": [
        "tag",
        "jp-tag",
        "indent-level",
        "blocks"
    ]
}