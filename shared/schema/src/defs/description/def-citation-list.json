{
    "$comment": "tags under description that directly contain paragraph-blocks",
    "type": "object",
    "properties": {
        "tag": {
            "type": "string",
            "const": "citation-list"
        },
        "jp-tag": {
            "type": "string"
        },
        "indent-level": {
            "type": "string"
        },
        "blocks": {
            "type": "array",
            "items": {
                "$comment": "blocks contains 'header' element. currently ignored, due to lack of no data using heading",
                "anyOf": [
                    {
                        "$ref": "#/$defs/paragraph"
                    },
                    {
                        "$ref": "#/$defs/citation-list-items"
                    }
                ]
            }
        }
    },
    "required": [
        "tag",
        "jp-tag",
        "indent-level",
        "blocks"
    ]
}