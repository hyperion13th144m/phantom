{
    "$comment": "temporarily, those tags are in this same type. tags will be divided based on rendering styles",
    "type": "object",
    "properties": {
        "tag": {
            "type": "string",
            "enum": [
                "jp:draft-person-group",
                "jp:article-group"
            ]
        },
        "jp-tag": {
            "type": "string"
        },
        "blocks": {
            "type": "array",
            "items": {
                "anyOf": [
                    {
                        "$ref": "#/$defs/notice-items-type-a"
                    }
                ]
            }
        }
    },
    "required": [
        "tag",
        "jp-tag",
        "blocks"
    ]
}