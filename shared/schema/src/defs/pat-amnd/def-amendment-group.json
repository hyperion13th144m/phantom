{
    "$comment": "TODO: check jp:receipt-number, jp:submission-date under this tag.",
    "type": "object",
    "properties": {
        "tag": {
            "type": "string",
            "const": "jp:amendment-group"
        },
        "jp-tag": {
            "type": "string"
        },
        "indent-level": {
            "type": "string"
        },
        "blocks": {
            "type": "array",
            "items": {
                "anyOf": [
                    {
                        "$ref": "#/$defs/bibliographic-items-type-a"
                    },
                    {
                        "$ref": "#/$defs/contents-of-amendment"
                    }
                ]
            }
        }
    },
    "required": [
        "tag",
        "jp-tag",
        "indent-level",
        "blocks"
    ]
}