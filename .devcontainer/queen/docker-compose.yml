services:
  queen-dev:
    build:
      context: services/queen
      dockerfile: Dockerfile.dev
    user: pyuser
    volumes:
      - .:/workspaces:cached
    tty: true
    entrypoint: ["sh", "-c", "sleep infinity"]
