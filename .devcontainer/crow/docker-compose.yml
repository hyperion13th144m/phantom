services:
  crow-dev:
    build:
      context: services/crow
      dockerfile: Dockerfile.dev
    user: pyuser
    volumes:
      - .:/workspaces:cached
    tty: true
    entrypoint: ["sh", "-c", "sleep infinity"]
