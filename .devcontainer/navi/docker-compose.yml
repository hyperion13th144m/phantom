services:
  navi-dev:
    build:
      context: services/navi
      dockerfile: Dockerfile.dev
    user: pyuser
    volumes:
      - .:/workspaces:cached
    tty: true
    entrypoint: ["sh", "-c", "sleep infinity"]
