services:
  panther-dev:
    build:
      context: services/panther
      dockerfile: Dockerfile.dev
    volumes:
      - .:/workspaces:cached
    tty: true
    entrypoint: ["sh", "-c", "sleep infinity"]
