services:
  mona-dev:
    build:
      context: services/mona
      dockerfile: Dockerfile.dev
    user: pyuser
    volumes:
      - .:/workspaces:cached
    tty: true
    entrypoint: ["sh", "-c", "sleep infinity"]
