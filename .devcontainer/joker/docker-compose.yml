services:
  joker-dev:
    build:
      context: services/joker
      dockerfile: Dockerfile.dev
    user: node
    tty: true
    volumes:
      - .:/workspaces:cached
    command: sh -c "sleep infinity"
