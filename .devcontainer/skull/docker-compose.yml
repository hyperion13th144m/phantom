services:
  skull-dev:
    build:
      context: services/skull
      dockerfile: Dockerfile.dev
    user: node
    tty: true
    volumes:
      - .:/workspaces:cached
    command: sh -c "sleep infinity"
