services:
  fox-dev:
    build:
      context: services/fox
      dockerfile: Dockerfile.dev
    user: node
    tty: true
    volumes:
      - .:/workspaces:cached
    command: sh -c "sleep infinity"
